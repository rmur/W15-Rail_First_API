<%= form_for(@add_other_show_detail) do |f| %>
  <% if @add_other_show_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@add_other_show_detail.errors.count, "error") %> prohibited this add_other_show_detail from being saved:</h2>

      <ul>
      <% @add_other_show_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :series %><br>
    <%= f.number_field :series %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.text_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
